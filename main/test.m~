%% initialization
clear;
close all;
clc;
load train_test_data.mat 
rng(1);

%%

n_center_vec = 24   
[idx, C] = kmeans(X_train, n_center_vec);
    
% Calulate sigma 
n_data = size(X_train,1);
        
K = zeros(n_center_vec, 1)

for i=1:n_center_vec
  K(i) = numel(find(idx == i));  %% 找到所对应的cluster
  
end

K